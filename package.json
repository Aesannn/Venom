{
  "name": "aether-os",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "core/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/shell",
    "build": "npm run build --workspace=apps/shell",
    "lint": "npm run lint --workspace=apps/shell"
  },
  "dependencies": {
    "@google/genai": "^2.7.0"
  }
}
